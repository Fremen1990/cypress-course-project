// yarn add -D @faker-js/faker
// const {faker} = require('@faker-js/faker');

describe("Faker Docket Post Test example", () => {
  it("Random Data", () => {
    cy.task("freshUser").then((object) => {
      cy.log("Data: ", object);
    });
  });

  it("Random Data", () => {
    cy.task("freshUser").then((object) => {
      cy.log("Data: ", object);
    });
  });

  it("Random Data", () => {
    cy.task("freshUser").then((object) => {
      cy.log("Data: ", object);
    });
  });

  it("Random Data", () => {
    cy.task("freshUser").then((object) => {
      cy.log("Data: ", object);
    });
  });

  it("Random Data", () => {
    cy.task("freshUser").then((object) => {
      cy.log("Data: ", object);
    });
  });
});
